---
plugin: aws_ec2
regions:
  - us-east-1
filters:
  tag:Environment: dev
  tag:Service: rewards
  instance-state-name: running
keyed_groups:
  - key: tags.Environment
    prefix: env
  - key: tags.Service
    prefix: service
hostnames:
  - instance-id
compose:
  ansible_host: instance_id
